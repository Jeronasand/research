# Pending Research

Create one directory per pending research topic:

```text
research/pending/<topic-slug>/
  README.md
```

Each topic README should record:

- status
- requested output
- known inputs
- open questions
- target HTML filename when known

When the research is complete, add the final HTML document under `research/` and refresh `research/private-index.json`.
