# 已完结调研

每个直接子目录都是一个已完成的 Web 静态调研包。

```text
research/completed/<research-id>/
  index.html
  assets/
  data/
  research.json   # optional metadata
```

前端以卡片展示包目录，点击卡片后预览该目录的 `index.html`，并提供返回目录按钮。
